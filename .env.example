# Required when LLM_MOCK=false
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Optional: Massive/Polygon API key for real market data
# If not set, uses built-in simulator
MASSIVE_API_KEY=

# Set to "true" for mock LLM responses (dev/testing without API key)
LLM_MOCK=false
